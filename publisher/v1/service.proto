syntax = "proto3";

package publisher.v1;

import "google/protobuf/wrappers.proto";

option go_package = "github.com/yoshihio/go-memps-proto/publisher/v1";

service PublisherService {
  rpc Publish(PublishRequest) returns (PublishResponse);
  rpc PublishBytes(PublishBytesRequest) returns (PublishBytesResponse);
}

message PublishRequest {
  string topic = 1;
  string message = 2;
}

message PublishResponse {
  google.protobuf.BoolValue success = 1;
}

message PublishBytesRequest {
  string topic = 1;
  bytes message = 2;
}

message PublishBytesResponse {
  google.protobuf.BoolValue success = 1;
}
